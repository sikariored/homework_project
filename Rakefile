# Add your own ticket_tasks in files placed in lib/ticket_tasks ending in .rake,
# for example lib/ticket_tasks/capistrano.rake, and they will automatically be available to Rake.

require_relative "config/application"

Rails.application.load_tasks
