# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no line_own_columns defined. If you add line_own_columns to the
# model remove the "{}" from the fixture names and add the line_own_columns immediately
# below each fixture, per the syntax in the comments below
#
one: {}
# column: value
#
two: {}
# column: value
